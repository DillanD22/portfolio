// Currently empty — this is here so the <script> tag in index.html has
// somewhere to point. A natural next step once the site is live could be
// a tag filter for the project cards (e.g. click "javascript" to only show
// JS projects). Ask Claude if/when you want to build that together.
